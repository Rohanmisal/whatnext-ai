# ── API Server ────────────────────────────────────────────────
# URL of the Express backend (default: localhost in dev)
VITE_API_URL=http://localhost:3001

# ── Supabase (Frontend / Auth) ────────────────────────────────
# Get these from: Supabase Dashboard → Project Settings → API
# Use the "anon / public" key here — NOT the service role key
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# ── App ───────────────────────────────────────────────────────
# Optional — OAuth callbacks use window.location.origin automatically
VITE_SENTRY_DSN=

# ── Legacy (server-side only, kept for vite.config.ts define) ─
GEMINI_API_KEY=
